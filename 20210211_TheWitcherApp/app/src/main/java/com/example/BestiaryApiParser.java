package com.example;

import org.json.JSONArray;
import org.json.JSONObject;

import java.util.ArrayList;
import java.util.List;

public class BestiaryApiParser {

    /*public static List<MonsterType> parseMonsterTypes(String json){

        JSONObject arrel = new JSONObject(json);
        JSONArray results = arrel.getJSONArray("results");
        List<Pokemon> pokemons = new ArrayList<>();
        for(int i=0;i< results.length();i++) {
            JSONObject pokemon = results.getJSONObject(i);
            Pokemon p = new Pokemon();
            p.name =  pokemon.getString("name");
            p.url =  pokemon.getString("url");
            loadImatgePokemon(p);
            pokemons.add(p);
        }
        return pokemons;

    }*/

}
